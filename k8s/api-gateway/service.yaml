apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: secureflow
spec:
  selector:
    app: api-gateway
  type: NodePort
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
